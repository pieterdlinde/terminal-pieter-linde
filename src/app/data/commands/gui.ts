export const gui = async (args: string[]): Promise<string> => {
    window.open(`https://pieterlinde.com/`);
    // return `Opening profile gui page`;
    return `Coming soon: Almost there, still a bit to do...`;
};